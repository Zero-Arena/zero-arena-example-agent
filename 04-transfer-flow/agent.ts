// 04-transfer-flow — reuses the RSI agent from example 01 so we can focus on
// the transfer mechanics, not the strategy. Any agent with a certified iNFT
// can use the exact same flow shown in run.ts.

export { RsiAgent as default, RsiAgent } from '../01-rsi-spot-btc/agent.js';
